--- a/upstream/smaug1.8/src/act_comm.c	2007-05-28 21:11:41.000000000 +0000
+++ b/upstream/smaug1.8/src/act_comm.c	2026-04-30 10:53:46.256890915 +0000
@@ -2768,8 +2768,7 @@
     char BUFF[MAX_STRING_LENGTH*2];
 
     if ((rpfile = fopen(RIPSCREEN_FILE,"r")) !=NULL) {
-      while ((BUFF[num]=fgetc(rpfile)) != EOF)
-	 num++;
+      { int _c; while ((_c = fgetc(rpfile)) != EOF) BUFF[num++] = _c; }
       fclose(rpfile);
       BUFF[num] = 0;
       write_to_buffer(ch->desc,BUFF,num);
@@ -2783,8 +2782,7 @@
     char BUFF[MAX_STRING_LENGTH*2];
 
     if ((rpfile = fopen(RIPTITLE_FILE,"r")) !=NULL) {
-      while ((BUFF[num]=fgetc(rpfile)) != EOF)
-	 num++;
+      { int _c; while ((_c = fgetc(rpfile)) != EOF) BUFF[num++] = _c; }
       fclose(rpfile);
       BUFF[num] = 0;
       write_to_buffer(ch->desc,BUFF,num);
@@ -2798,8 +2796,7 @@
     char BUFF[MAX_STRING_LENGTH*2];
 
     if ((rpfile = fopen(ANSITITLE_FILE,"r")) !=NULL) {
-      while ((BUFF[num]=fgetc(rpfile)) != EOF)
-	 num++;
+      { int _c; while ((_c = fgetc(rpfile)) != EOF) BUFF[num++] = _c; }
       fclose(rpfile);
       BUFF[num] = 0;
       write_to_buffer(ch->desc,BUFF,num);
@@ -2813,8 +2810,7 @@
     char BUFF[MAX_STRING_LENGTH];
 
     if ((rpfile = fopen(ASCTITLE_FILE,"r")) !=NULL) {
-      while ((BUFF[num]=fgetc(rpfile)) != EOF)
-	 num++;
+      { int _c; while ((_c = fgetc(rpfile)) != EOF) BUFF[num++] = _c; }
       fclose(rpfile);
       BUFF[num] = 0;
       write_to_buffer(ch->desc,BUFF,num);
--- a/upstream/smaug1.8/src/db.c	2006-10-09 12:42:48.000000000 +0000
+++ b/upstream/smaug1.8/src/db.c	2026-04-30 10:53:56.516891792 +0000
@@ -4285,11 +4285,7 @@
     {
       while ( !feof(fp) )
       {
-	while ((buf[num]=fgetc(fp)) != EOF
-	&&      buf[num] != '\n'
-	&&      buf[num] != '\r'
-	&&      num < (MAX_STRING_LENGTH-2))
-	  num++;
+	{ int _c; while ((_c = fgetc(fp)) != EOF && _c != '\n' && _c != '\r' && num < (MAX_STRING_LENGTH-2)) buf[num++] = _c; if (num > 0) buf[num] = _c; }
 	c = fgetc(fp);
 	if ( (c != '\n' && c != '\r') || c == buf[num] )
 	  ungetc(c, fp);
@@ -4323,11 +4319,7 @@
     {
       while ( !feof(fp) )
       {
-	while ((buf[num]=fgetc(fp)) != EOF
-	&&      buf[num] != '\n'
-	&&      buf[num] != '\r'
-	&&      num < (MAX_STRING_LENGTH-2))
-	  num++;
+	{ int _c; while ((_c = fgetc(fp)) != EOF && _c != '\n' && _c != '\r' && num < (MAX_STRING_LENGTH-2)) buf[num++] = _c; if (num > 0) buf[num] = _c; }
 	c = fgetc(fp);
 	if ( (c != '\n' && c != '\r') || c == buf[num] )
 	  ungetc(c, fp);
